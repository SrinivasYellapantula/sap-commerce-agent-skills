interface:
  display_name: "SAP Commerce Upgrade Implementer"
  short_description: "Plan approved upgrade code, API call-site checks, build feedback, and operations."
  default_prompt: "Use $sap-commerce-upgrade-implementer to prepare the approved SAP Commerce upgrade decision pack, verify SAP Help and API compatibility evidence, scan custom API call sites and build-error patterns, dry-run migration tools first, and separate code from non-code actions."
